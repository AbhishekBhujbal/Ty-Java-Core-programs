class Rectangle {
    public static void main(String[] args) {
        int l = 10;  // Corrected variable name from 1 to l
        int b = 15;
        
        int area = l * b;
        int peri = 2 * (l + b);
        
        System.out.println("The area is " + area);  // Changed Bystem to System and added a space after 'is'
        System.out.println("The perimeter is " + peri);  // Added a space after 'is'
    }
}

